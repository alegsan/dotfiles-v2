# ~/.zshrc.local — machine-specific overrides (profile: {{ profile }})
# Generated by Ansible — do not edit manually.

# ── fzf (brew) — tell oh-my-zsh fzf plugin where to find it ───────────────────────
export FZF_BASE="{{ brew_prefix }}/opt/fzf"

{% if profile == 'work' %}
# ── Work machine extras ─────────────────────────────────────────────────────────────────
# export HTTP_PROXY="http://proxy.company.com:8080"
# export HTTPS_PROXY="$HTTP_PROXY"
# export NO_PROXY="localhost,127.0.0.1"
{% endif %}

{% if profile == 'private' %}
# ── Private machine extras ───────────────────────────────────────────────────────────
{% endif %}

