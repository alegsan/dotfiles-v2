---
# Run against localhost — this repo is always executed locally after bootstrap.sh
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
